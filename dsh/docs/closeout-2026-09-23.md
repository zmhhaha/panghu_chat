# DSH closeout evidence (2026-09-23)

## Changes applied

`k8s/resourcequotas.yaml` passed server dry-run and was applied live without
restarting workloads. Runner namespace admission budget: two Pods, two PVCs,
40Gi requested storage, 2 CPUs and 4Gi memory in aggregate limits. Web namespace
budget: two Pods, two PVCs, 10Gi storage, 3 CPUs and 5Gi memory in aggregate
limits. These are admission budgets, not new per-Pod limits or PID controls.
Additional projects and temporary PVC recovery drills must fit these budgets
or use a separately reviewed namespace/budget.

## Network verification

Fixed `verify-network-boundary.sh` to obtain the node InternalIP rather than
attempt DNS resolution of the Kubernetes node name. Also support stdin execution.
The corrected script completed with exit code 0 against the actual runner.

- LLM, RAG, embedding, PostgreSQL, Redis, Kubernetes API and Vault: timeout.
- Metadata 169.254.169.254:80 and node 192.168.137.211:22: timeout.
- npm mirror, GitHub and Casdoor public TCP destinations: connected.
- Internal and public DNS resolution: successful.
- IPv6 public probe: ENETUNREACH; the runner still has link-local IPv6.

Public TCP success is not a verified download: the transparent proxy may accept
connections before reaching the remote server. IPv6 link-local and proxy-mediated
inward routing are not accepted by this test and remain open.

## Runtime controls and outstanding work

Runner UID 10000, effective capabilities zero, NoNewPrivs=1.
Actual cgroup pids.max is 19116: a cap exists, but it has not been accepted as an
appropriate project budget. Do not describe PID enforcement as absent or complete.

The SSH host identity and project commands still share UID 10000. A read-only key
mount cannot prevent key reads; identity separation needs a validated transport
change before replacing the working runner. Cancellation, project workflow,
persistence/restore and live allowlist revocation tests remain outstanding.

Both production workloads remained Ready (web 2/2, runner 1/1); no restart or
production PVC modification was performed in this pass.

## Allowlist revocation regression

All 10 adapter tests passed, including an added established-stream revocation
scenario. With valid simulated OAuth and native cookies, replacing the allowlist
to remove the owner rejects subsequent HTTP requests and WebSocket handshakes
with 401. The already upgraded connection still echoes data. Restoring the owner
allows a new handshake without restarting the adapter. Empty lists deny access;
missing lists return 503. These are isolated adapter tests with simulated OAuth
and upstream servers, not a real Casdoor account-revocation test.

Production source hashes match the locally tested source exactly:

- adapter.mjs: `7bf323f34de07407b14276c168e9d446638988de4892a25e7edead4950dcfeb8`
- owners.mjs: `562c23c7429cddd20fa22431b589176567fd330c0dce94edea367c7ede6ee8c8`

No production allowlist, account or session was changed. Actual ConfigMap volume
propagation with a dedicated authenticated test user remains an end-to-end
acceptance item. Revocation is enforced on the next request/handshake after file
propagation, not by terminating established streams or already running commands.

## Process and project checks

The installed SSH helper's real process provider was exercised inside the runner:
an ordinary command spawned a `sleep` child, `terminate()` was called, and the
child was gone after `waitForExit` (exit 0). Browser cancellation, detached or
reparented processes and SSH disconnect cancellation were not claimed.

The project workflow test was attempted in a fresh `/workspace` audit directory.
The runner could reach GitHub over HTTPS (HEAD returned 301) and the npm mirror
(HEAD returned 302), but `git ls-remote` and clone received an authentication
challenge from the egress path. Therefore public clone, dependency install,
local commit and restore remain unverified; no existing project files changed.

The runner remains UID 10000 with no effective capabilities, `NoNewPrivs=1`, and
RuntimeDefault. Namespace quotas are live. SSH identity separation, a suitable
PID budget, IPv6 and proxy-mediated inward routing remain open.

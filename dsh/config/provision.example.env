# Copy to provision.local.env. Trusted operator configuration.
# Node the project work container lands on. Must have enough memory for the
# project's build/test workload; see docs/operations.md.
NODE_HOSTNAME=orangepi5-max-server1
# Per-project workspace and dependency volume.
WORKSPACE_SIZE=20Gi

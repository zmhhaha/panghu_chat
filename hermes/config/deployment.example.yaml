# Copy to deployment.local.yaml. Supply actual immutable image digests.
image: arm-cluster-master:5000/hermes-intelligence@sha256:REPLACE
oauth_image: quay.io/oauth2-proxy/oauth2-proxy@sha256:REPLACE
hostname: hermes.panghuer.top
owner_email: REPLACE
oidc_issuer: https://REPLACE
storage_class: REPLACE
web_storage: 10Gi
report_storage: 10Gi
# Both report and web pods use this node to support RWO local storage.
node_hostname: REPLACE
# Label the existing cloudflared POD TEMPLATE hermes-ingress=true.
# MFA is configured in the IdP, not by this manifest.
